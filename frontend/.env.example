# PAISAI frontend environment (example).
# Copy to .env.local for local development. Never commit real values.
# Not required for the foundational landing build.

# Base URL of the PAISAI backend API (FastAPI). Public — safe to expose.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
