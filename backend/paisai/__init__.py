"""PAISAI backend — an AI Financial Operating System.

The defining property of PAISAI is trust. This package makes the founding
principles (see the repository ``docs/``) structural rather than aspirational:

- ``paisai.integrity`` encodes the no-hallucination policy, the six provenance
  categories, epistemic labelling, and the required anatomy of an answer.
- ``paisai.engine`` provides deterministic financial math so that every
  ``Calculated`` value is reproducible and auditable — numbers never originate
  from a language model's memory.
"""

__version__ = "0.1.0"
